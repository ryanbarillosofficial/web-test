'use client';
import React from 'react'

const AddToCart = () => {
    return (
        <div>
            <button onClick={() => {alert("Hello"); console.log("Hello");} }>Add to Cart</button>
        </div>
    )
}

export default AddToCart